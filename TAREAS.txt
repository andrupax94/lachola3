BACKEND
*-----Hacer que las solicitudes getEventos incluyan el encabezado autorization con el bearer token.
*-----crear un midleware que verifique si existe el bearer token y esta validado.
*-----con la tarea anterior hay que crear saveImg para almacenar las imagenes al wordpress(en postman creo que tengo un ejemplo).
*-----crear saveEvents en eventoscontroller para que guarde en el wordpress los eventos seleccionados(en postman creo que tengo un ejemplo por individual),
*-----crear una ruta que se envarge de renovar las variables en el cache eventos y eventosG
en cuanto a sobrescribir sensillamente se añaden elementos iguales siempre y cuando tengan diferentes fuentes, si son iguales las fuentes se sobrescribe.
*?????verificar numero maximo de solicitudes en laravel para evitar error por limite de la mismas
*arreglar movibeta.
*introducir mensajes de estado para la animacion de carga del front.
*Hacer que el plugin(lacholaplugin) envie a el backend de laravel(setToken) el token bearer.
*revisar si existen dependencias COMPOSER que no uso.
*optimizar y quitar codigo redundante.

FRONEND
*-----hacer que la aplicacion angular verifique si existe el token de acceso, solicitando a getToken en userController.
*-----hacer que extraer Eventos guarde datos seleccionados a saveEvents para ello hace falta que funcionen los checkbox a lado de los eventos.
*-----crear funcionalidad para saveEvents All que envia todos los eventos y no solo los checkeados.
*-----crear un servicio que se envarge de renovar las variables en el cache eventos y eventosG
*-----terminar de arreglar la paginacion que teniendo 15 paginas no muestra ni la ultima ni las que vienen despues de 10.
*-----hay una redundancia de codigo cuando el componente filter llama a verEventos para mostrarlos.
*implementar la animacion de carga con los mensajes que arroja el backend.
*ver los modales que ya he hecho para ver si existe un mensaje de confirmacion.
*configurar comportamiento al no haber un token que muestre el mensaje de error y tenga un boton para reintentar;
*revisar si existen dependencias NPM que no uso.
*+++++optimizar y quitar codigo redundante.

WORDPRESS

OTROS
*----configurar el plugin de ocultar elementos para mayor organizacion.
*revisar si existen archivos inutiles.

PARA UN FUTURo
crear un ttask en php que elimine eventos que ya pasaron con sus respectivas imagenes.
