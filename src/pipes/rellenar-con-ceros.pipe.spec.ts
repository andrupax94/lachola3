import { RellenarConCerosPipe } from './rellenar-con-ceros.pipe';

describe('RellenarConCerosPipe', () => {
  it('create an instance', () => {
    const pipe = new RellenarConCerosPipe();
    expect(pipe).toBeTruthy();
  });
});
