import { QuitarCaracterPipe } from './quitar-caracter.pipe';

describe('QuitarCaracterPipe', () => {
  it('create an instance', () => {
    const pipe = new QuitarCaracterPipe();
    expect(pipe).toBeTruthy();
  });
});
