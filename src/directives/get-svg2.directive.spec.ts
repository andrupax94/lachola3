import { GetSvg2Directive } from './get-svg2.directive';

describe('GetSvg2Directive', () => {
  it('should create an instance', () => {
    const directive = new GetSvg2Directive();
    expect(directive).toBeTruthy();
  });
});
