import { NoSpaceDirective } from './noSpace.directive';

describe('NgEnterDirective', () => {
  it('should create an instance', () => {
    const directive = new NoSpaceDirective();
    expect(directive).toBeTruthy();
  });
});
