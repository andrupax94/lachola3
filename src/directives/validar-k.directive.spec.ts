import { ValidarKDirective } from './validar-k.directive';

describe('ValidarKDirective', () => {
  it('should create an instance', () => {
    const directive = new ValidarKDirective();
    expect(directive).toBeTruthy();
  });
});
