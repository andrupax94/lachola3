import { FileReadDirective } from './file-read.directive';

describe('FileReadDirective', () => {
  it('should create an instance', () => {
    const directive = new FileReadDirective();
    expect(directive).toBeTruthy();
  });
});
