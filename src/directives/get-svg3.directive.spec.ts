import { GetSvg3Directive } from './get-svg3.directive';

describe('GetSvg3Directive', () => {
  it('should create an instance', () => {
    const directive = new GetSvg3Directive();
    expect(directive).toBeTruthy();
  });
});
