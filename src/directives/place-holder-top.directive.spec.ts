import { PlaceHolderTopDirective } from './place-holder-top.directive';

describe('PlaceHolderTopDirective', () => {
  it('should create an instance', () => {
    const directive = new PlaceHolderTopDirective();
    expect(directive).toBeTruthy();
  });
});
