import { GetsvgDirective } from './getsvg.directive';

describe('GetsvgDirective', () => {
  it('should create an instance', () => {
    const directive = new GetsvgDirective();
    expect(directive).toBeTruthy();
  });
});
